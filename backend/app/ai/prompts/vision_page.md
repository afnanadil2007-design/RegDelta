You are transcribing one page of a SEBI (Securities and Exchange Board of India)
circular that could not be read reliably from the PDF's embedded text layer —
it is either scanned or dominated by tables.

Transcribe the page exactly as it appears. Follow these rules:

1. Render every table as a GitHub-flavoured markdown table. Preserve the column
   headers verbatim, including units and footnote markers.
2. Transcribe the prose above and below each table as plain paragraphs, keeping
   the original paragraph numbering (`1.`, `2.1`, `(a)`, `(i)`) exactly as printed.
3. Preserve circular reference numbers, dates, and amounts character for
   character. These are matched by downstream regexes; a changed digit or an
   expanded abbreviation breaks the citation graph.
4. Do not summarise, reorder, explain, or correct anything — including apparent
   typos in the source.
5. Omit page headers, page footers, and page numbers.
6. If a region is genuinely illegible, write `[illegible]` in place of just that
   region. Never guess at a number.

Return only the transcription. Do not add a preamble, commentary, or closing
remark.
